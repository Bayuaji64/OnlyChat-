const router = require('express').Router()
const Controller = require('../controllers')


router.get('/', )

router.get('/:jobId/detail',)

router.get('/:jobId/apply', )
router.post('/:jobId/apply',)

router.get('/:jobId/apply/:applicantId/accept',)
router.get('/:jobId/apply/:applicantId/reject',)



module.exports = router